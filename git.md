CANAL INITCODE

configurou o git na maquina com os seguintes comendos:

git config --global user.name "<nome>"  
git config --global user.email "<email>"

configuramos a chave ssh

Salvar arquivo = ctrl + S
Atalho para abrir a pasta = ctrl + K + O

em javascript Quando é um texto eu preciso usar aspas duplas ou imples

texto = string
juntar texto = concatenar